// The lowest package: diagnostics and subdomain error types. Depends only on
// core, so every higher package can import it without creating a cycle.

import {
  // `extends.mbt` names `@debug.Debug` to decline its promotion.
  "moonbitlang/core/debug",
}
