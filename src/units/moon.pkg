// Type-safe value newtypes shared across the pipeline: cell references, colours,
// and date/time serials.
// Pure (core only) so it can sit near the bottom of the dependency graph next to
// `diag`.
