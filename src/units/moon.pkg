// Type-safe value newtypes shared across the pipeline (§4): cell references
// today, colours and dimensions as later phases need them. Pure (core only) so
// it can sit near the bottom of the dependency graph next to `diag`.
